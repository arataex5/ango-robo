import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.arataex5.angorobo',
  appName: 'アンゴウロボ',
  webDir: 'dist',
  android: { backgroundColor: '#FFF6E5' },
};

export default config;
